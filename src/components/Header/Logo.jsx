import React from "react";
import style from './Header.module.css';

function Logo({}){

    return (
        <div className={style.logo}>
        POST GROUND
        </div>
    );
}

export default Logo;